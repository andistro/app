#!/bin/bash
trap 'rm -f $HOME/andistro-bridge; exit 0' TERM INT
while true; do
    if [ -f $HOME/andistro-bridge ] && [ -s $HOME/andistro-bridge ]; then
        CMD=$(cat $HOME/andistro-bridge)
        rm $HOME/andistro-bridge
        sh -c "$CMD"
    fi
    sleep 0.5
done
